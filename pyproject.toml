[build-system]
requires      = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name            = "regulaite"
version         = "1.0.0"
description     = "Multi-jurisdiction AI-risk copilot"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]          # <── our code is in src/regulaite
